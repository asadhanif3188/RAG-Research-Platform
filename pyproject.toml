# Workspace root configuration — workspace members are in [tool.uv.workspace]

[tool.uv.sources]
shared = { workspace = true }
fastest-rag = { workspace = true }
multimodal-rag = { workspace = true }
corrective-rag = { workspace = true }
self-rag = { workspace = true }
video-rag = { workspace = true }
api = { workspace = true }

[tool.uv.workspace]
members = [
    "shared",
    "pipelines/fastest_rag",
    "pipelines/multimodal_rag",
    "pipelines/corrective_rag",
    "pipelines/self_rag",
    "pipelines/video_rag",
    "api",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-cov>=5.0",
    "testcontainers[postgres,redis]>=4.7",
    "httpx>=0.27",
    "factory-boy>=3.3",
    "freezegun>=1.5",
    "ruff>=0.15.16",
    "mypy>=1.11",
    "psycopg2-binary>=2.9",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = [
    "shared/tests",
    "pipelines/fastest_rag/tests",
    "pipelines/multimodal_rag/tests",
    "pipelines/corrective_rag/tests",
    "pipelines/self_rag/tests",
    "pipelines/video_rag/tests",
]
addopts = "--import-mode=importlib --cov=shared/src --cov=pipelines/fastest_rag/src --cov=pipelines/multimodal_rag/src --cov=pipelines/corrective_rag/src --cov=pipelines/self_rag/src --cov=pipelines/video_rag/src --cov-report=term-missing"
markers = [
    "unit: Unit tests (no external dependencies)",
    "integration: Integration tests (require Docker services)",
]

# Ruff config in ruff.toml, mypy config in mypy.ini
